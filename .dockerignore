# Build context is the repo root (see deploy/docker-compose.yml). Keep the
# image build fast by excluding everything the Dockerfile doesn't COPY.
**/node_modules
**/dist
.git
.claude
**/.env
**/.env.*
!**/.env.production
**/*.local
**/npm-debug.log*
**/*.md
